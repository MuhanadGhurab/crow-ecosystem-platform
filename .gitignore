# OS
.DS_Store
Thumbs.db
Desktop.ini

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Next.js / Node
node_modules/
.next/
out/
.env
.env*.local
.env.staging
.env.production
.env.production.runtime
.env.production.operator
.env.production.pulled.tmp
.env.vercel

# Prisma
prisma/*.db
prisma/*.db-journal

# Secrets & credentials
credentials.json
*.pem
.supabase/

# Private strategy — keep in repo; exclude from public mirror only (see docs/internal/PUBLIC_GITHUB.md)
# docs/internal/ is tracked in the private monorepo; do NOT gitignore here.

# Vercel CLI
.vercel/
.env.vercel.local

# C2.1 read-only Preview audit evidence (masked; local only)
docs/architecture/crow-core/c2/.c21-preview-audit-evidence.json
docs/architecture/crow-core/c2/.c22-legacy-blueprint-ownership.local.json

# TypeScript incremental build cache (local only)
tsconfig.tsbuildinfo

# Cursor / agent local state (repo policy: do not commit)
.agents/
skills-lock.json
.vercel

# C3 pre-migration backups (local operator artifacts; never commit)
.backups/

# C3 Preview env pulls and operator secrets (never commit)
.env.staging.runtime
.env.preview.proof-link
.env.preview.*
.env.preview.e2e
.env.migration.recovery
.migration-apply-manifest

# FTGP Platform Admin bootstrap operator artifacts (never commit)
.ftgp-platform-admin-candidates.local.json
.ftgp-platform-admin-bootstrap-manifest
.env.platform-bootstrap.operator

# Ad-hoc local debug / test artifacts
tmp-legal.html

# Internal preview E2E evidence (local operator; keep out of public mirror)
docs/internal/screenshots/c3-preview-e2e/
docs/internal/C3.*-PREVIEW-E2E-REPORT.md

# C3 browser session raw evidence (sanitized summary committed separately)
docs/internal/c3-browser-session-certification/C3_10D_BROWSER_SESSION_REPORT.json
docs/internal/screenshots/c3-preview-email-only-proof/

# Ad-hoc C3 debug scripts (local operator tooling; not product surface)
scripts/check-c3-*.ts
scripts/smoke-c3-provision-hosted.ts
scripts/smoke-supabase-admin-hosted.ts
scripts/staging-resend-key-digest.ts
scripts/sync-preview-resend-key.ts
scripts/test-otp-crack.ts
scripts/test-otp-email-send.ts
scripts/test-preview-from-resend.ts

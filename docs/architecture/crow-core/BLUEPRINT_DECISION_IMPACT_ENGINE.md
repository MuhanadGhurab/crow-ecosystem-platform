# Blueprint Decision Impact Engine

**Status:** CURRENT (session-local preview)

## API

`analyzeBlueprintDecisionImpact()`, `applyDecisionToSessionDraft()`, `revertSessionDecision()`

## Constraints

Deterministic, immutable original compile, session-local draft only, advisory, no authority application.

## UI

Blueprint Studio Decisions mode + `StudioDecisionTimeline` — displays PREVIEW ONLY / NOT PERSISTED.

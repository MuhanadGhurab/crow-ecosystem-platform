# Business Field Complete Inventory (CROW.UAT.1)

Machine-readable catalog: `src/lib/business-field-catalog/fields.ts`

**After UAT.1:** 115 client-facing fields (was 99). Placeholder `custom_specialist_activity` removed — custom paths use `CUSTOM_UNRESOLVED`.

Each field includes: stable key, English/Arabic names, category, aliases, misspellings, examples, purpose hints, capability hints, SSIC/ISIC/NAICS crosswalk metadata where applicable.

Audit classifications used during expansion: `WELL_DEFINED`, `MISSING_ALIASES`, `MISSING_ARABIC` (resolved where justified), new fields fully specified (no placeholders).

# Department Archetype Catalog

**Status:** CURRENT

28 department archetypes in `src/lib/model-forge/departments/department-archetype-catalog.ts`.

Each defines purpose, responsibilities, recommended Work Personas, workflow/entity ownership, KPIs, SAREA pattern, CyberCrow policies, scale merge/split candidates.

`grantsPermissions: false` on every entry.

Departments merge at MICRO scale and split at ENTERPRISE scale (advisory only).

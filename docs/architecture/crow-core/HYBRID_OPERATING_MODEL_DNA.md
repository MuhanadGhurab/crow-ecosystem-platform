# Hybrid Operating Model DNA

**Status:** CURRENT

## API

`composeEnterpriseModel(HybridCompositionInput)` → `EnterpriseModelDraft` with `OrganizationalModelDNA`

## Hybrid inputs

`primaryIndustry`, `secondaryIndustries`, `specialistDomains`, `organizationalOverlays`, `scaleProfile`, `topologies`, `selectedCapabilities`, `organizationSignals`

## Reference hybrids

10 models in `src/lib/model-forge/references/hybrid-reference-models.ts`

## DNA fields

Primary industry, specialist domains, topology, scale, workforce model, external actors, authority style, workflow intensity, trust profile, experience strategy, provenance trail

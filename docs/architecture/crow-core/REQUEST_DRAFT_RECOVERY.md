# Request Draft Recovery (CROW.UAT.1)

Device-local draft in `draft-storage.ts` — schema `request-wizard-draft-v1`, TTL 7 days.

UI: **Resume draft** / **Discard draft** / auto-save notice ("Draft saved on this device").

Clears on successful submission and sign-out. Does not overwrite submitted requests.

No server persistence — honest labeling in UI.

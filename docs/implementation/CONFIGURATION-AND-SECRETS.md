# Configuration and secrets

Names only: `GHURAVIA_RUNTIME_MODE`, `GHURAVIA_DATABASE_URL`, `GHURAVIA_APP_VERSION`, `GHURAVIA_LOCAL_CONFIRM`, `GHURAVIA_SYNTHETIC_SESSION_SECRET`. Do not track `.env` files; diagnostics redact database URLs. Session secret is local/test only (min 16 chars).

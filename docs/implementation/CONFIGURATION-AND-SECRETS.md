# Configuration and secrets

Names only: `GHURAVIA_RUNTIME_MODE`, `GHURAVIA_DATABASE_URL`, `GHURAVIA_APP_VERSION`, `GHURAVIA_LOCAL_CONFIRM`. Do not track `.env` files; diagnostics redact database URLs.

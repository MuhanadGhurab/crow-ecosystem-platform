# Testing and CI

Run `npm run ci`. CI is read-only, has no deploy job, and uses no production secrets. Invariants cover activation, progression, evidence, trust, audit intent, route governance, and configuration guards.

#!/usr/bin/env npx tsx
console.error("platform-owner:bootstrap-execute is disabled pending separate product-owner authorization.");
console.error("Use npm run platform-owner:bootstrap-plan for dry-run validation only.");
process.exit(1);

import { CaseStudiesDeferredPageContent } from "@/components/public-site/public-canonical-pages-part2";

export const metadata = {
  title: "Case Studies — Crow",
  description: "Client evidence when approved — no fabricated proof.",
};

export default function CaseStudiesPage() {
  return <CaseStudiesDeferredPageContent />;
}

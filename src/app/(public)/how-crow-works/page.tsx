import { HowCrowWorksPageContent } from "@/components/public-site/public-canonical-pages-part1";

export const metadata = {
  title: "How Crow Works",
  description: "The governed Crow lifecycle from request through runtime.",
};

export default function HowCrowWorksPage() {
  return <HowCrowWorksPageContent />;
}

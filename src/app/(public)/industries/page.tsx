import { IndustriesPageContent } from "@/components/public-site/public-canonical-pages-part2";

export const metadata = {
  title: "Industries — Crow",
  description: "Operating models across sectors — not module packages.",
};

export default function IndustriesPage() {
  return <IndustriesPageContent />;
}

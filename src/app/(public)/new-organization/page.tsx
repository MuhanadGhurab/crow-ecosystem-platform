import { NewOrganizationPageContent } from "@/components/public-site/public-canonical-pages-part1";

export const metadata = {
  title: "Build a New Organization — Crow",
  description: "Design a new operating model from intent through Enterprise Blueprint.",
};

export default function NewOrganizationPage() {
  return <NewOrganizationPageContent />;
}

import { PublicHomepage } from "@/components/public-site/public-homepage";

export const metadata = {
  title: "Crow — Design how your organization should operate",
  description:
    "Crow turns organizational intent into an Enterprise Blueprint and operational tenant — governed from request through runtime.",
};

export default function HomePage() {
  return <PublicHomepage />;
}

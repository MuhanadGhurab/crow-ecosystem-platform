import { PlatformCemPageContent } from "@/components/public-site/public-canonical-pages-part2";

export const metadata = {
  title: "CEM — Crow Platform",
  description: "Operational work engine for responsibilities, workflows, and outcomes.",
};

export default function PlatformCemPage() {
  return <PlatformCemPageContent />;
}

import { PlatformCybercrowPageContent } from "@/components/public-site/public-canonical-pages-part2";

export const metadata = {
  title: "CyberCrow — Crow Platform",
  description: "Operational trust inside the tenant.",
};

export default function PlatformCybercrowPage() {
  return <PlatformCybercrowPageContent />;
}

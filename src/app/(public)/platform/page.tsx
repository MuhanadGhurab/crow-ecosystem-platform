import { PlatformOverviewPageContent } from "@/components/public-site/public-canonical-pages-part2";

export const metadata = {
  title: "Platform — Crow",
  description: "One governed foundation: CEM, CyberCrow, SAREA, and ProCrow.",
};

export default function PlatformPage() {
  return <PlatformOverviewPageContent />;
}

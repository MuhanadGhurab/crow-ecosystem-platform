import { PlatformProcrowPageContent } from "@/components/public-site/public-canonical-pages-part2";

export const metadata = {
  title: "ProCrow — Crow Platform",
  description: "Lifecycle governance and accountable intelligence.",
};

export default function PlatformProcrowPage() {
  return <PlatformProcrowPageContent />;
}

import { PlatformSareaPageContent } from "@/components/public-site/public-canonical-pages-part2";

export const metadata = {
  title: "SAREA — Crow Platform",
  description: "Permitted experience adaptation — presentation, not permission.",
};

export default function PlatformSareaPage() {
  return <PlatformSareaPageContent />;
}

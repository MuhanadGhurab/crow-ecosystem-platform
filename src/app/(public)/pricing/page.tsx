import { PricingPageContent } from "@/components/public-site/public-canonical-pages-part2";

export const metadata = {
  title: "Commercial Model — Crow",
  description: "Scope-aware commercial model — implementation and monthly tenant subscription.",
};

export default function PricingPage() {
  return <PricingPageContent />;
}

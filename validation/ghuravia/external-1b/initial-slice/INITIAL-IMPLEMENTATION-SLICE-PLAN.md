# Initial Implementation Slice Plan

Following GHV.IMPLEMENTATION.0A only, the first slice is a local-only skeleton: bounded contracts, a deny-by-default activation boundary, a local database adapter with migration ownership, audited state transition primitives, and provider adapter ports using mocks. It excludes Preview, production, public accounts, uploads, payments, Live Sky, provider credentials, and any launch feature.

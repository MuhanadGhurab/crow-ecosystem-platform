# Local Database Contract Validation

This harness uses only the ephemeral PostgreSQL instance identified by `GHV_VAL_1B_DATABASE_URL`. All objects are placed in `ghv_validation_1b`, identified as validation-only, and dropped by the script.

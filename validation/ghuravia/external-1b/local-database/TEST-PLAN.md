# Test Plan

Apply the isolated SQL contract, then exercise unique idempotency, activation transition, optimistic concurrency, append-only event/audit rows, derived ledger, outbox, recursive graph traversal, Arabic Unicode, `timestamptz`, scoped recalculation, transaction rollback, and schema cleanup. The script must exit nonzero on any failed assertion.
